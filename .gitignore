# Xcode
xcuserdata/
DerivedData/
*.xcuserstate

# Local dev tooling (idb, installed via uv — see README). The venv itself is
# disposable and reproduced from mcp/idb/uv.lock; the lockfile is tracked.
mcp/idb/.venv/
