[project]
name = "gratitude-idb-tooling"
version = "0.1.0"
description = "Pinned idb (iOS Debug Bridge) client, used by the ios-simulator MCP server for interactive Simulator testing. Not part of the app itself — see README.md."
requires-python = ">=3.10"
dependencies = [
    "fb-idb==1.5.2",
]
